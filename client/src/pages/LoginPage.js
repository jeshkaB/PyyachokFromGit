import {LoginForm, LoginFormForgot} from '../components';

const LoginPage = ()=> {

    return (
        <div>
               <LoginForm/>
               <LoginFormForgot/>
        </div>
    );
};

export {LoginPage};
