import {LoginForm} from "../components";
import {useSelector} from "react-redux";

function LoginPage() {

    return (
        <div>

            <div>
               <LoginForm/>
            </div>

        </div>
    );
}

export {LoginPage};
